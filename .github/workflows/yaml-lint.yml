# yaml lint
---
name: Cookiecutter Template YAML Lint

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  yaml-lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install yaml-lint
        run: |
          make install

      - name: Render Cookiecutter Template
        run: |
          make render-cookiecutter
      - name: Lint YAML files in rendered template
        run: |
          make yaml-lint-check
